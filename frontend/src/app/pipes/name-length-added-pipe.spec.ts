import { NameLengthAddedPipe } from './name-length-added-pipe';

describe('NameLengthAddedPipe', () => {
  it('create an instance', () => {
    const pipe = new NameLengthAddedPipe();
    expect(pipe).toBeTruthy();
  });
});
