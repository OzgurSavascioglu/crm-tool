import { CustomerAddedPipe } from './customer-added-pipe';

describe('CustomerAddedPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomerAddedPipe();
    expect(pipe).toBeTruthy();
  });
});
